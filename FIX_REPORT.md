# FIX REPORT — Линейная Алгебра: Экзамен

## Проверено и исправлено

- 27 страниц билетов: `tickets/ticket-01.html` ... `tickets/ticket-27.html`.
- Удалён режим устного ответа.
- Исправлен баг рендера `\relax`: в математике используется `\mathbb{C}`.
- Название сайта изменено на `Линейная Алгебра: Экзамен`.
- Добавлены примеры в каждый билет.
- `formulas.html`: 105 строк формул, минимум 3 на билет.
- `trainer.html`: 108 карточек, минимум 3 на билет.
- `interactives.html`: 6 интерактивов.
- Проверены локальные ссылки: битых ссылок нет.

## Как применить

Скопировать содержимое архива в корень репозитория, заменить старые файлы, затем:

```bash
git add .
git commit -m "Fix exam site render, trainer, formulas and interactives"
git push
```

После push GitHub Actions сам пересоберёт Pages.
